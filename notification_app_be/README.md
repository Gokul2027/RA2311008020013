This folder is intentionally present to match the broader pre-test repository structure.

The dedicated submission in this workspace is frontend-focused:

- `priority_inbox/` contains the Stage 1 ranking task.
- `notification_app_fe/` contains the Stage 2 React application.

No separate backend implementation is required for this dedicated frontend assignment.
